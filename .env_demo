# OpenAI API key for the pathway analysis agent
OPENAI_API_KEY=your_openai_api_key_here

# Cache directory for storing API responses (optional)
CACHE_DIR=materials/query_cache

# Model to use for pathway analysis (default: gpt-4)
PATHWAY_AGENT_MODEL=gpt-4

CACHE_TTL=-1  # -1 means infinite (never expire)